name: Hello World 

on:
  workflow_dispatch:

jobs:
  hello-world-job:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Print Hello
      run: echo "Hello Harshavardhan"
